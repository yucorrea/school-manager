{% extends "layout.njk" %}

{% block content %}
  <div class="card">
      <section class="avatar" style="background: url({{teacher.avatar}}) no-repeat center center / cover;"></section>
    <form method="POST" action="/teachers?_method=PUT">

      <section class="details">

        <h3>EDITAR PROFESSOR</h3>
        
        {% include "teachers/fields.njk" %}
        
        <input type="text" name="id" value="{{teacher.id}}" hidden>
    </form>

    <form method="POST" action="/teachers?_method=DELETE" id="form-delete">
          <input type="text" name="id" value="{{teacher.id}}" hidden="hidden">
          <button type="submit" class="button-delete">Deletar</button>
    </form>

</section>

 
  </div>
  {% include "../delete.njk" %}
  
  {%endblock%}